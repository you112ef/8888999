<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude AI model files from backup -->
    <exclude domain="file" path="sperm_analyzer.torchscript.pt"/>
    <!-- Exclude cache files -->
    <exclude domain="cache" path="."/>
</full-backup-content>